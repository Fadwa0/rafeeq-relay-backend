services:
  - type: web
    name: rafeeq-relay
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn rafeeq_relay:app --bind 0.0.0.0:$PORT --workers 1 --threads 4 --timeout 120
